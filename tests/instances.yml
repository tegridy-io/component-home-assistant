# Overwrite parameters here

parameters:
  instances:
    config:
      ingress:
        enabled: true
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt-production
      extra:
        enabled: true
